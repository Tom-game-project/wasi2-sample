package gas:drive-app@0.1.0-alpha;

interface gas-file {
	resource gas-file {
		constructor();
		get-name: func() -> string;
	}
}

interface gas-drive-app {
	use gas-file.{gas-file};
	resource gas-drive-app {
		constructor();
		get-file-by-id: func(id :string) -> gas-file;
		get-storage-used: func() -> s64;
	}
}

world imports {
	import gas-file;
	import gas-drive-app;
}
